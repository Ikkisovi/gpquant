# Time-Series Adaptive Line Strategy

## 概述

这是一个完全基于**时间序列**（无截面泄漏）的量化策略框架，使用遗传规划（GP）自动发现最优的"自适应线" f(t)，然后通过固定的映射规则将价格偏离转换为仓位。

### 核心创新

1. **纯时间序列方法**：所有特征和信号都按单资产独立计算，避免未来信息泄漏
2. **自适应线 vs 固定策略**：GP搜索线 f(t)，策略映射固定为 `w = tanh(zscore((price-f)/price))`
3. **信息夏普适应度**：策略相对基准的超额收益Sharpe，而非绝对Sharpe
4. **严格跟踪约束**：
   - 相对回撤 ≤ 基准+5%
   - 绝对超额 > 0（必须打败buy-and-hold）
5. **复杂度与换手惩罚**：防止过拟合和过度交易

---

## 系统架构

```
gpquant_ext/
├── ops_ts.py           # 时序算子（15个新增）
├── features.py         # 特征工程（18个特征）
├── strategy_ts.py      # 固定策略模板
├── fitness.py          # 信息夏普+约束
├── benchmark.py        # 等权B&H基准
├── dataloader.py       # 数据加载+Walk-forward
└── trainer.py          # 主训练循环

train_ts_adaptive.py    # 入口脚本
config_ts_adaptive.yaml # 配置文件
```

---

## 数据要求

CSV文件：`daily_am_pm_data.csv`

必须字段：
- `symbol`: 股票代码
- `timestamp`: 时间戳（半日粒度：AM/PM）
- `open`, `high`, `low`, `close`: OHLC价格
- `volume`: 成交量
- `vwap`: 成交均价
- `turnover`: 换手率
- `mktcap`: 市值

示例格式：
```
symbol,timestamp,open,high,low,close,volume,vwap,turnover,mktcap
agx,2023-01-03 12:00:00-05:00,36.83,37.30,36.80,36.91,13920,36.99,0.0010,509765897
agx,2023-01-03 16:00:00-05:00,36.89,37.00,36.61,36.71,42668,36.78,0.0031,507022918
...
```

---

## 特征工程

系统自动生成18个特征（全部时间序列，无截面）：

### 价格相关
- `price_to_vwap`: 价格偏离VWAP
- `vwap_slope`: VWAP动量（标准化）
- `ret_5`, `ret_20`, `ret_60`: 不同周期收益率

### 稳健动量
- `mom_rank_ts`: 稳健动量在自身历史中的分位数（0-1）
  - 使用Winsorize + Huber变换
  - 结合20期收益与EMA

### 流动性与市值
- `turnover_z`: 换手率Z-score（时间序列）
- `mcap_z`: 对数市值Z-score（时间序列）

### 风险指标
- `sortino_5/20/60`: 下行Sortino比率（三个窗口）
- `dd_log_5/20/60`: 对数回撤（三个窗口）
- `vol_20/60`: 年化波动率（两个窗口）

### 原始数据
- `close`, `volume`, `vwap`: 供GP直接使用

---

## 时序算子

注册了15个新算子（`ops_ts.py`）：

### 基础时序
```python
ts_lag(x, n)         # 滞后n期
ts_ema(x, n)         # 指数移动平均
ts_sma(x, n)         # 简单移动平均
ts_std(x, n)         # 滚动标准差
ts_zscore(x, n)      # 滚动Z-score
ts_decay(x, n)       # 线性衰减加权
ts_pct_change(x, n)  # n期涨跌幅
```

### 风险度量
```python
ts_sortino(x, n)         # Sortino比率
ts_drawdown_log(x, n)    # 对数回撤
ts_max_dd(x, n)          # 最大回撤
ts_rank_pct(x, n)        # 滚动分位数(0-1)
```

### 价量派生
```python
price_to_vwap(price, vwap)  # 价格偏离
vwap_slope(vwap, n)         # VWAP斜率
```

### 稳健变换
```python
winsorize(x)         # Winsorize截尾
huber(x)             # Huber变换
```

---

## 策略模板（固定不变）

GP只搜索线 `f(t)`，策略映射是固定的：

```python
# Step 1: 偏离度
g = (price - f) / price

# Step 2: 标准化
z = zscore(g, lookback=40)

# Step 3: 单调映射到仓位
w_raw = tanh(k * z)

# Step 4: 风险调整
w = w_raw / rolling_vol(60)

# Step 5: 归一化 + t-1执行
w = normalize(w).shift(1)

# Step 6: 扣除交易成本
net = port_return - cost_bps * turnover
```

**关键**：这是A用法（时间序列），价格在线上 → 做多，线下 → 做空。

---

## 适应度函数

```python
Fitness = IS - penalties

IS = mean(excess) / std(excess) * sqrt(504)  # 信息夏普

penalties:
  1. λ_mdd * max(0, MDD_rel - 0.05)          # 相对回撤超5%
  2. γ_excess * |excess| if excess ≤ 0       # 未打败基准
  3. δ_complexity * formula_depth            # 公式复杂度
  4. ε_turnover * mean(turnover)             # 换手率
```

**硬约束模式**（可选）：任何违规直接返回 -999（淘汰）

---

## Walk-Forward验证

```
训练窗口: 12个月
验证窗口: 1个月
Embargo: 10个半日bar

[------Train------][Embargo][--Val--]
                            ↓
                   [------Train------][Embargo][--Val--]
                                                ↓
                                       [------Train------][Embargo][--Val--]
```

每个窗口输出：
- 信息夏普
- 相对MDD
- 绝对超额
- 约束满足情况
- 换手率

---

## 使用方法

### 1. 安装依赖

```bash
pip install pandas numpy pyyaml numba
# gpquant 应该已经安装
```

### 2. 准备数据

将 `daily_am_pm_data.csv` 放在项目根目录

### 3. 配置参数

编辑 `config_ts_adaptive.yaml`:

```yaml
# 关键参数
train_months: 12           # 训练窗口
val_months: 1              # 验证窗口
population_size: 200       # GP种群大小
generations: 30            # 进化代数
cap_mdd: 0.05              # 最大相对回撤5%
```

### 4. 运行训练

```bash
python train_ts_adaptive.py
```

或指定配置：
```bash
python train_ts_adaptive.py --config my_config.yaml --data my_data.csv
```

### 5. 查看结果

结果保存在 `results_ts_adaptive.csv`：

```csv
fold,formula,formula_complexity,train_fitness,information_sharpe,relative_mdd,meets_all_constraints,...
0,ts_ema(mom_rank_ts, 20),12,1.85,1.72,0.042,True,...
1,div(ret_20, ts_std(close, 30)),18,1.68,1.55,0.038,True,...
...
```

---

## 输出示例

```
============================================================
Walk-Forward Summary
============================================================

Fold 0:
  Information Sharpe:     1.7234
  Relative MDD:           4.2%     ✓ PASS
  Excess Cumulative:      8.5%     ✓ PASS
  Formula: ts_ema(mom_rank_ts, 20)

Fold 1:
  Information Sharpe:     1.5512
  Relative MDD:           3.8%     ✓ PASS
  Excess Cumulative:      6.2%     ✓ PASS
  Formula: div(ret_20, ts_std(close, 30))

...

============================================================
Aggregate Statistics
============================================================

Information Sharpe:
  Mean:  1.6425
  Std:   0.1234
  Min:   1.2105
  Max:   1.9876

Constraint Satisfaction:
  MDD Constraint:      95.0% of folds  ✓
  Excess Constraint:   90.0% of folds  ✓
  All Constraints:     85.0% of folds  ✓

Mean Turnover:         0.1523
```

---

## 核心优势

### 1. 无未来信息泄漏
- ✅ 所有特征按单资产独立计算
- ✅ 无横截面排名/标准化
- ✅ 严格t-1执行

### 2. 可解释性强
- ✅ 策略逻辑固定且直观
- ✅ GP公式可读（如：`ts_ema(mom_rank_ts, 20)`）
- ✅ 参数含义明确

### 3. 约束明确
- ✅ 必须打败等权B&H
- ✅ 回撤控制在基准+5%以内
- ✅ 防止过拟合（复杂度惩罚）
- ✅ 控制交易成本（换手惩罚）

### 4. Walk-Forward稳健
- ✅ 滚动训练+验证
- ✅ Embargo防止信息泄漏
- ✅ 多窗口统计汇总

---

## 调优建议

### 提升性能

**增加搜索空间**：
```yaml
population_size: 500    # 100→500
generations: 50         # 30→50
```

**调整策略参数**：
```yaml
z_L: 30                 # 40→30 (更快反应)
k_tanh: 1.5             # 1.0→1.5 (更激进)
```

**放松约束**（如果太严格）：
```yaml
cap_mdd: 0.08           # 0.05→0.08 (允许8%相对回撤)
lambda_mdd: 2.0         # 3.0→2.0 (降低惩罚)
```

### 降低过拟合

**更强的简约性**：
```yaml
parsimony_coefficient: 0.005  # 0.001→0.005
delta_complexity: 0.01        # 0.001→0.01
```

**更长的训练窗口**：
```yaml
train_months: 18        # 12→18
```

---

## 故障排除

### 问题1：所有公式fitness = -999

**原因**：约束太严格，没有个体能满足

**解决**：
1. 设置 `hard_constraints: false`（使用软惩罚）
2. 放宽 `cap_mdd` 到 0.08-0.10
3. 增加 `population_size`

### 问题2：公式过于复杂

**原因**：复杂度惩罚太弱

**解决**：
```yaml
parsimony_coefficient: 0.01
delta_complexity: 0.01
init_depth: [2, 4]  # 降低初始深度
```

### 问题3：策略几乎不交易

**原因**：`k_tanh` 太小或 `z_L` 太长

**解决**：
```yaml
k_tanh: 2.0        # 增强信号强度
z_L: 20            # 缩短窗口
```

### 问题4：无法打败基准

**原因**：数据可能处于盘整期，或特征不够

**解决**：
1. 检查数据质量和时间范围
2. 尝试不同的训练窗口
3. 考虑添加更多技术指标到features.py

---

## 与原三线策略对比

| 维度 | 原三线策略 | 时序自适应线 |
|-----|-----------|------------|
| **目标** | 绝对Sharpe | 信息Sharpe（超额） |
| **基准** | 无（或被动提及） | 等权B&H（强制对比） |
| **约束** | 软约束（建议） | 硬/软约束（强制） |
| **策略逻辑** | GP搜索（策略也进化） | 固定映射（只搜索线） |
| **特征** | OHLCV基础 | 18个时序特征 |
| **算子** | 68个（通用） | 68+15（时序专用） |
| **可迁移性** | 中等 | 高（逻辑固定） |
| **可解释性** | 中等 | 高（线+偏离） |

---

## 文件清单

| 文件 | 行数 | 功能 |
|-----|------|------|
| `ops_ts.py` | ~230 | 时序算子定义与注册 |
| `features.py` | ~240 | 特征工程（18个特征） |
| `strategy_ts.py` | ~140 | 固定策略模板 |
| `fitness.py` | ~200 | 信息夏普+约束适应度 |
| `benchmark.py` | ~120 | 等权B&H基准 |
| `dataloader.py` | ~230 | 数据加载+Walk-forward |
| `trainer.py` | ~260 | 主训练循环 |
| `train_ts_adaptive.py` | ~110 | 入口脚本 |
| `config_ts_adaptive.yaml` | ~80 | 配置文件 |

**总计**: ~1600行核心代码

---

## 下一步

1. ✅ 运行 `python train_ts_adaptive.py` 开始第一次训练
2. 📊 分析 `results_ts_adaptive.csv` 中的结果
3. 🔧 根据结果调整 `config_ts_adaptive.yaml` 参数
4. 🔄 多次迭代优化
5. 📈 在测试集上验证最终策略

---

## 常见问题

**Q: 为什么用信息夏普而不是绝对夏普？**

A: 信息夏普衡量相对基准的超额收益，确保策略**真正增加价值**而不是靠市场beta。

**Q: 为什么策略逻辑固定？**

A: 固定的单调映射（线上做多、线下做空）更稳定、更可迁移。GP只搜索那条"最优的线"。

**Q: 5%相对回撤限制合理吗？**

A: 这是保守设置，实际可根据需求调整。关键是**相对**基准，而非绝对。

**Q: 训练需要多久？**

A: 取决于配置：
- Population=200, Generations=30: 约30-60分钟/fold
- Population=500, Generations=50: 约2-3小时/fold

**Q: 可以用在其他数据上吗？**

A: 可以！只需确保CSV包含必需字段。时间粒度可以不同（调整`ann_K`）。

---

## 技术支持

问题或建议：
1. 查看代码注释（每个函数都有详细docstring）
2. 检查 `REAL_TRAINING_RESULTS.md` 中的真实案例
3. 参考 `GPQUANT_PIPELINE_EXPLAINED.md` 理解GP机制

**祝训练顺利！** 🚀
